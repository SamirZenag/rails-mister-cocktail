<div class="banner">
  <div class="banner-content">
    <h1>Mister Z</h1>
    <p> Cocktails Creation</p>
    <span>
      <%= link_to 'EXPLORE', new_cocktail_path, class: 'btn btn-primary' %>
    </span>
  </div>
</div>

<ul class="list-unstyled">
  <% @cocktails.each do |cocktail| %>
    <li><%= link_to cocktail.name, cocktail_path(cocktail) %></li>
  <% end %>
</ul>
